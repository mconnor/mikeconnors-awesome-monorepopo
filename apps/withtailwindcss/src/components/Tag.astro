---
interface TagProps {
  name: string;
  link?: string;
}

const { name, link = '#' } = Astro.props;
---

<span
  class="mb-2 mr-2 inline-block rounded-full bg-blue-200 px-3 py-1 text-sm font-semibold text-blue-700"
  >{name}</span
>
