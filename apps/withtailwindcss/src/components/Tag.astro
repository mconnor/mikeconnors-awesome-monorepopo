---
interface TagProps {
  name: string;
  link?: string;
}

const { name, link = '#' } = Astro.props;
---

<span
  class="mr-2 mb-2 inline-block rounded-full bg-blue-200 px-3 py-1 text-sm font-semibold text-blue-700"
  >{name}</span
>
