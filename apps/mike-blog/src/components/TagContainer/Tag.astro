---
// import Icon from '@repo/layout-webcomponents/Icon';
// import IconLink from '@repo/astro-ui/IconLink';
interface Props {
  tag: string;
  count: number;
  del: number;
  /** Index of tag for staggered animation delay */
  index?: number;
}
const { tag, count, index = 0 } = Astro.props;

// Use the actual Unicode character (U+E478)
const copy = `${tag}: ${count}`;
// Calculate animation delay based on index (100ms per tag)
const animationDelay = `${index * 100}ms`;
---

<a
  href={`/tags/${tag}`}
  class="btn whitespace-nowrap transition delay-150 duration-300 ease-in-out btn-primary hover:-translate-y-1 hover:scale-110 hover:btn-secondary"
>
  <i class="ph ph-tag"></i>
  <span class="text-xl">{tag}</span><span class="badge badge-xl">{count}</span>
</a>

<!-- <button
  class="btn bg-blue-500 transition delay-150 duration-300 ease-in-out hover:-translate-y-1 hover:scale-110 hover:bg-indigo-500 ..."
>
  Save Changes
</button> -->
