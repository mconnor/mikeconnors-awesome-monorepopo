---
import { Image, inferRemoteSize } from 'astro:assets';

interface Props {
  src: string;
  alt: string;
}
const { src, alt } = Astro.props;

const { width, height } = await inferRemoteSize(src);
---

<Image slot="image" width={width} height={height} src={src} alt={alt} />
