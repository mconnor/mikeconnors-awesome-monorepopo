---
import Capsule from './Capsule.astro';

interface Item {
  label: string;
  year: number;
}
interface Props {
  items: Item[];
}

const { items } = Astro.props;
---

<ul class="group timeline timeline-vertical md:timeline-horizontal">
  {
    items.map((item, index) => (
      <Capsule
        label={item.label}
        year={item.year}
        first={index === 0}
        last={index === items.length - 1}
      />
    ))
  }
</ul>
