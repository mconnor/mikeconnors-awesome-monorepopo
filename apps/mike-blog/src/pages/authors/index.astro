---
import { getCollection } from 'astro:content';
import Layout from '#layouts/Layout.astro';
const authors = await getCollection('authors');
// console.log('+++++++authors');
// console.log({ ...authors });
// console.log('-----authors');
// console.log('+++++++authors');

// const { data: authorsData } = authorsObj;
// authorsData.map((d) => console.log({ ...d }));
// console.log('-----authors');
---

<Layout title="authors">
  <!-- <Debug {tag} />
    <Debug {posts} /> -->
  <!-- <p>{authors.length}</p> -->
  <p>authors</p>
  {
    authors.map((author) => (
      <div class="flex items-center gap-2">
        <a href={`/authors/${author.id}`}>{author.data.name}</a>
      </div>
    ))
  }
</Layout>
