---
// import Center from '@repo/layout-webcomponents/Center';
// import Cover from '@repo/layout-webcomponents/Cover';
import type { RmUnitType } from '@repo/layout-webcomponents/Types';
import Prose from '#components/Prose.astro';
// import Header from '#components/Header/index.astro';
import NavBar from '#components/NavBar/index.astro';

import Footer from '#components/Footer/index.astro';
import BaseLayout from './BaseLayout.astro';
// import Nav from '@repo/astro-ui/NavBarRes';
// import { slide } from 'astro:transitions';
interface Props {
  maxW?: RmUnitType;
  children: unknown;
  title?: string;
  description?: string;
}

const { maxW = '60ch', ...rest } = Astro.props;
---

<BaseLayout {...rest}>
  <div class="flex min-h-screen flex-col">
    <NavBar />
    <main
      class="m-auto flex flex-col items-center justify-center p-4 lg:max-w-6/10"
    >
      <slot />
    </main>
    <Footer />
    <!-- <footer class="mt-auto flex items-center justify-center p-4">
      <p class="text-sm text-gray-500">
        © {new Date().getFullYear()} Mike Connor. All rights reserved.
      </p>
    </footer> -->
  </div>
</BaseLayout>
