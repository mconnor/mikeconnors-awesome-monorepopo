---
import Center from '@repo/layout-webcomponents/Center';
import Cover from '@repo/layout-webcomponents/Cover';
import type { RmUnitType } from '@repo/styles/Types';

import Footer from '#components/Footer.astro';
import Header from '#components/Header.astro';

import Layout from './BaseLayout.astro';

interface Props {
  maxW?: RmUnitType;
  children: unknown;
  title?: string;
  description?: string;
}

const {
  maxW = 'var(--measure, 60ch)',
  title,
  description,
  ...rest
} = Astro.props;
---

<Layout title={title} description={description} {...rest} maxW="60ch">
  <div
    class="prose md:prose-lg lg:prose-xl dark:prose-invert prose-a:text-blue-600 prose-a:hover:text-blue-500"
  >
    <slot />
  </div>
</Layout>
