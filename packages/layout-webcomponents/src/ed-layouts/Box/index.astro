---
import './Box.css';

import type { BorderStyle, RmUnitType } from '@repo/styles/Types';

interface Props {
  padding?: RmUnitType;
  borderWidth?: string; // stick with px, rem, em.
  // highlight?: boolean;
  // borderStyle?: BorderStyle;
  invert?: boolean;
  children?: unknown;
}
const props = Astro.props;
---


<div>
  <box-l {...props}>
    <slot />
  </box-l>
</div>


<script src="./Box.js"></script>