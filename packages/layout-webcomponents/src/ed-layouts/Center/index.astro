---
import './Center.css';

import type { RmUnitType } from '#Types.ts';

interface Props {
  max?: RmUnitType;
  andText?: boolean;
  gutters?: boolean;
  intrinsic?: boolean;
}

const props = Astro.props;
---

<script src="./Center.js"></script>

<center-l {...props}>
  <slot />
</center-l>
