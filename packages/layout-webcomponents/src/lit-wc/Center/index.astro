---
import { Center } from './center-l';
import type { RmUnitType } from '@repo/styles/Types';

interface Props {
  max?: RmUnitType;
  andText?: boolean;
  gutters?: RmUnitType;
  intrinsic?: boolean;
  children: unknown;
}
const props = Astro.props;
---

<Center {...props} client:visible>
  <slot />
</Center>

<script src="./center-l"></script>
