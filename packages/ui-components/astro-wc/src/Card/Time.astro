---
import { parseISO, format } from 'date-fns';

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time
  class="border-1 relative order-first text-sm text-gray-600"
  dateTime={date.to}>{format(date, 'LLLL	d, yyyy')}</time
>
