---
import Icon from '#Icon/index.astro';
interface Props {
  twClass: string;
}

const { twClass = 'text-3xl' } = Astro.props;
---

<astro-sun-moon>
  <div id="group">
    <button type="button"
      ><Icon
        icon="sun"
        size="var(--s1)"
        color="var(--color-primary-200)"
        gradient={false}
      /></button
    >
    <button type="button"></button>
    <Icon
      icon="moon-stars"
      size="var(--s1)"
      color="var(--color-primary-200)"
      gradient={false}
    />
  </div>
</astro-sun-moon>

<script>
  import 'SunMoon.ts';
</script>
