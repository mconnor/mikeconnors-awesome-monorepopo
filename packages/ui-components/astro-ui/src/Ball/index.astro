---
interface Props {
  className?: string;
  size: string;
}

const { size, className = 'bg-base-200' } = Astro.props;
---

<div class:list={[`${size} rounded-full ${className}`]}>
  <h2 class="text-base-content">{className}</h2>
</div>
