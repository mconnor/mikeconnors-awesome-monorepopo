---
// import { cn } from '@repo/utils/cn';
interface Props {
  cta?: string;
  title?: string;
  copy: string;
  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
}

const {
  cta = 'Buy Now',
  title = 'title',
  size = 'xs',
  copy,
} = Astro.props as Props;

const cardSize = 'd-card-' + size;

// class:list will work

// const cardVariants = {
//     xs: "d-card w-96 bg-base-100 d-card-xs shadow-sm",
//     sm: "d-card w-96 bg-base-100 d-card-sm shadow-sm",
//     md: "d-card w-96 bg-base-100 d-card-md shadow-sm",
//     lg: "d-card w-96 bg-base-100 d-card-lg shadow-sm",
//     xl: "d-card w-96 bg-base-100 d-card-xl shadow-sm",
//   };


---
<div class:list={['d-card w-96 bg-base-100 shadow-sm',  {cardSize}]}>
<!-- <div class={cardVariants[size]}> -->
  <div class="d-card-body">
    <h2 class="d-card-title">{title}</h2>
    <p>
     xx {copy}
    </p>

  <div class="justify-end d-card-actions">
<button class="d-btn d-btn-primary">Buy Now</button>
    </div>
  </div>
</div>
