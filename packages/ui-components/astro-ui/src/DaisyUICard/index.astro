---
interface Props {
  cta?: string;
  href?: string;
  title?: string;
  copy: string;
  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
}

const {
  cta = 'Buy Now',
  title = 'title',
  size = 'xs',
  copy,
  href = './',
} = Astro.props as Props;

const cardSize = 'card-' + size;

// class:list will work

// const cardVariants = {
//     xs: "card w-96 bg-base-100 card-xs shadow-sm",
//     sm: "card w-96 bg-base-100 card-sm shadow-sm",
//     md: "card w-96 bg-base-100 card-md shadow-sm",
//     lg: "card w-96 bg-base-100 card-lg shadow-sm",
//     xl: "card w-96 bg-base-100 card-xl shadow-sm",
//   };
---

<div class:list={['card w-96 bg-base-100 shadow-sm', { cardSize }]}>
  <!-- <div class={cardVariants[size]}> -->
  <div class="card-body">
    <h2 class="card-title">{title}</h2>
    <p>
      xx {copy}
    </p>

    <div class="card-actions justify-end">
      <a href={href} class="btn btn-primary">{cta}</a>
    </div>
  </div>
</div>
