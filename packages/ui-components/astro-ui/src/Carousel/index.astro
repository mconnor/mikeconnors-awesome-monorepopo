---
interface Props {
  images: {
    img: string;
    altText: string;
  }[];
}

const { images } = Astro.props;
---

<div class="carousel max-w-md carousel-center space-x-4 rounded-box bg-neutral">
  {
    images.map(({ img, altText }) => (
      <div class="carousel-item">
        <img src={img} class="rounded-box" alt={altText} />
      </div>
    ))
  }
</div>
